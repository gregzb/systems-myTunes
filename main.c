#include <stdio.h>
#include <stdlib.h>

#include "llist.h"

int main() {
    srand(time(NULL));

    initialize();

    // struct node * my_music = NULL;
    // my_music = insert_ordered(my_music, "oui", "a");
    // my_music = insert_ordered(my_music, "nein", "c");
    // my_music = insert_ordered(my_music, "alpha", "b");
    // my_music = insert_ordered(my_music, "nein", "a");
    // my_music = insert_ordered(my_music, "nein", "c");
    // print_list(my_music);
    //
    // struct node * found0 = find_node(my_music, "oui", "a");
    // struct node * found1 = find_node(my_music, "alpha", "b");
    // struct node * found2 = find_node_by_artist(my_music, "c");
    // print_list(found0);
    // print_list(found1);
    // print_list(found2);
    //
    // struct node * random = find_random(my_music);
    // print_list(random);
    // random = find_random(my_music);
    // print_list(random);
    // random = find_random(my_music);
    // print_list(random);
    // random = find_random(my_music);
    // print_list(random);
    // random = find_random(my_music);
    // print_list(random);
    // free_list(my_music);
    return 0;
}
